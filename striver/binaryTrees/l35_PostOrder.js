// Construct Unique BT using given inorder and postorder

const inorder = [40, 20, 50, 10, 60, 30]; //  left, root, right
const postorder = [40, 50, 20, 60, 30, 10]; // left, right, root

function constructBt(inorder, postorder) {
  // TODO

  return result;
}

const root = constructBt(inorder, postorder);
console.log('\nBT constructed is ', root);
